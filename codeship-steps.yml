- service: parser_test
  name: test
  command: "coverage run -m unittest discover && coveralls"

- service: parser
  type: push
  image_name: michaelbahr/eve-pos-parser
  image_tag: 1.0.0
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: service
  type: push
  image_name: michaelbahr/eve-pos-service
  image_tag: 1.0.0
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted